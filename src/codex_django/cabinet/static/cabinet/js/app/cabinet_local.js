/*
 * Local project JS overrides.
 * In your local project, configure compiler_config.json to output to "app/cabinet_local.js".
 * This placeholder prevents ManifestStaticFilesStorage errors in projects that don't compile JS.
 */
