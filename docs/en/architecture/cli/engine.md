<!-- DOC_TYPE: CONCEPT -->

# CLI Engine Moved

The Jinja-based scaffold engine now lives in `codex-django-cli`.

This runtime repository no longer documents that engine in detail so it can remain buildable and documentable without a sibling CLI checkout.
